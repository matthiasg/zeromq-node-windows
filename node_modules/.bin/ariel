#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../ariel/bin/ariel.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../ariel/bin/ariel.js" "$@"
  ret=$?
fi
exit $ret
